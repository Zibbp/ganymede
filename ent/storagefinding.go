// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/zibbp/ganymede/ent/storagefinding"
)

// StorageFinding is the model entity for the StorageFinding schema.
type StorageFinding struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// What kind of finding this is.
	Kind storagefinding.Kind `json:"kind,omitempty"`
	// Absolute path of the directory.
	Path string `json:"path,omitempty"`
	// Size of the directory in bytes.
	SizeBytes int64 `json:"size_bytes,omitempty"`
	// When the directory was first found.
	DetectedAt   time.Time `json:"detected_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StorageFinding) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case storagefinding.FieldSizeBytes:
			values[i] = new(sql.NullInt64)
		case storagefinding.FieldKind, storagefinding.FieldPath:
			values[i] = new(sql.NullString)
		case storagefinding.FieldDetectedAt:
			values[i] = new(sql.NullTime)
		case storagefinding.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StorageFinding fields.
func (_m *StorageFinding) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case storagefinding.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case storagefinding.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = storagefinding.Kind(value.String)
			}
		case storagefinding.FieldPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field path", values[i])
			} else if value.Valid {
				_m.Path = value.String
			}
		case storagefinding.FieldSizeBytes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field size_bytes", values[i])
			} else if value.Valid {
				_m.SizeBytes = value.Int64
			}
		case storagefinding.FieldDetectedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field detected_at", values[i])
			} else if value.Valid {
				_m.DetectedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StorageFinding.
// This includes values selected through modifiers, order, etc.
func (_m *StorageFinding) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this StorageFinding.
// Note that you need to call StorageFinding.Unwrap() before calling this method if this StorageFinding
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *StorageFinding) Update() *StorageFindingUpdateOne {
	return NewStorageFindingClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the StorageFinding entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *StorageFinding) Unwrap() *StorageFinding {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: StorageFinding is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *StorageFinding) String() string {
	var builder strings.Builder
	builder.WriteString("StorageFinding(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("kind=")
	builder.WriteString(fmt.Sprintf("%v", _m.Kind))
	builder.WriteString(", ")
	builder.WriteString("path=")
	builder.WriteString(_m.Path)
	builder.WriteString(", ")
	builder.WriteString("size_bytes=")
	builder.WriteString(fmt.Sprintf("%v", _m.SizeBytes))
	builder.WriteString(", ")
	builder.WriteString("detected_at=")
	builder.WriteString(_m.DetectedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// StorageFindings is a parsable slice of StorageFinding.
type StorageFindings []*StorageFinding
