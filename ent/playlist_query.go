// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/zibbp/ganymede/ent/multistreaminfo"
	"github.com/zibbp/ganymede/ent/playlist"
	"github.com/zibbp/ganymede/ent/playlistrulegroup"
	"github.com/zibbp/ganymede/ent/predicate"
	"github.com/zibbp/ganymede/ent/vod"
)

// PlaylistQuery is the builder for querying Playlist entities.
type PlaylistQuery struct {
	config
	ctx                 *QueryContext
	order               []playlist.OrderOption
	inters              []Interceptor
	predicates          []predicate.Playlist
	withVods            *VodQuery
	withMultistreamInfo *MultistreamInfoQuery
	withRuleGroups      *PlaylistRuleGroupQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlaylistQuery builder.
func (_q *PlaylistQuery) Where(ps ...predicate.Playlist) *PlaylistQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PlaylistQuery) Limit(limit int) *PlaylistQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PlaylistQuery) Offset(offset int) *PlaylistQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PlaylistQuery) Unique(unique bool) *PlaylistQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PlaylistQuery) Order(o ...playlist.OrderOption) *PlaylistQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryVods chains the current query on the "vods" edge.
func (_q *PlaylistQuery) QueryVods() *VodQuery {
	query := (&VodClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playlist.Table, playlist.FieldID, selector),
			sqlgraph.To(vod.Table, vod.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, playlist.VodsTable, playlist.VodsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMultistreamInfo chains the current query on the "multistream_info" edge.
func (_q *PlaylistQuery) QueryMultistreamInfo() *MultistreamInfoQuery {
	query := (&MultistreamInfoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playlist.Table, playlist.FieldID, selector),
			sqlgraph.To(multistreaminfo.Table, multistreaminfo.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, playlist.MultistreamInfoTable, playlist.MultistreamInfoColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRuleGroups chains the current query on the "rule_groups" edge.
func (_q *PlaylistQuery) QueryRuleGroups() *PlaylistRuleGroupQuery {
	query := (&PlaylistRuleGroupClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playlist.Table, playlist.FieldID, selector),
			sqlgraph.To(playlistrulegroup.Table, playlistrulegroup.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, playlist.RuleGroupsTable, playlist.RuleGroupsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Playlist entity from the query.
// Returns a *NotFoundError when no Playlist was found.
func (_q *PlaylistQuery) First(ctx context.Context) (*Playlist, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{playlist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PlaylistQuery) FirstX(ctx context.Context) *Playlist {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Playlist ID from the query.
// Returns a *NotFoundError when no Playlist ID was found.
func (_q *PlaylistQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{playlist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PlaylistQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Playlist entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Playlist entity is found.
// Returns a *NotFoundError when no Playlist entities are found.
func (_q *PlaylistQuery) Only(ctx context.Context) (*Playlist, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{playlist.Label}
	default:
		return nil, &NotSingularError{playlist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PlaylistQuery) OnlyX(ctx context.Context) *Playlist {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Playlist ID in the query.
// Returns a *NotSingularError when more than one Playlist ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PlaylistQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{playlist.Label}
	default:
		err = &NotSingularError{playlist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PlaylistQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Playlists.
func (_q *PlaylistQuery) All(ctx context.Context) ([]*Playlist, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Playlist, *PlaylistQuery]()
	return withInterceptors[[]*Playlist](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PlaylistQuery) AllX(ctx context.Context) []*Playlist {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Playlist IDs.
func (_q *PlaylistQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(playlist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PlaylistQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PlaylistQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PlaylistQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PlaylistQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PlaylistQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PlaylistQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlaylistQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PlaylistQuery) Clone() *PlaylistQuery {
	if _q == nil {
		return nil
	}
	return &PlaylistQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]playlist.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.Playlist{}, _q.predicates...),
		withVods:            _q.withVods.Clone(),
		withMultistreamInfo: _q.withMultistreamInfo.Clone(),
		withRuleGroups:      _q.withRuleGroups.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithVods tells the query-builder to eager-load the nodes that are connected to
// the "vods" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PlaylistQuery) WithVods(opts ...func(*VodQuery)) *PlaylistQuery {
	query := (&VodClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVods = query
	return _q
}

// WithMultistreamInfo tells the query-builder to eager-load the nodes that are connected to
// the "multistream_info" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PlaylistQuery) WithMultistreamInfo(opts ...func(*MultistreamInfoQuery)) *PlaylistQuery {
	query := (&MultistreamInfoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMultistreamInfo = query
	return _q
}

// WithRuleGroups tells the query-builder to eager-load the nodes that are connected to
// the "rule_groups" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PlaylistQuery) WithRuleGroups(opts ...func(*PlaylistRuleGroupQuery)) *PlaylistQuery {
	query := (&PlaylistRuleGroupClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRuleGroups = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Playlist.Query().
//		GroupBy(playlist.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *PlaylistQuery) GroupBy(field string, fields ...string) *PlaylistGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PlaylistGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = playlist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Playlist.Query().
//		Select(playlist.FieldName).
//		Scan(ctx, &v)
func (_q *PlaylistQuery) Select(fields ...string) *PlaylistSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PlaylistSelect{PlaylistQuery: _q}
	sbuild.label = playlist.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PlaylistSelect configured with the given aggregations.
func (_q *PlaylistQuery) Aggregate(fns ...AggregateFunc) *PlaylistSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PlaylistQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !playlist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PlaylistQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Playlist, error) {
	var (
		nodes       = []*Playlist{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withVods != nil,
			_q.withMultistreamInfo != nil,
			_q.withRuleGroups != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Playlist).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Playlist{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withVods; query != nil {
		if err := _q.loadVods(ctx, query, nodes,
			func(n *Playlist) { n.Edges.Vods = []*Vod{} },
			func(n *Playlist, e *Vod) { n.Edges.Vods = append(n.Edges.Vods, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMultistreamInfo; query != nil {
		if err := _q.loadMultistreamInfo(ctx, query, nodes,
			func(n *Playlist) { n.Edges.MultistreamInfo = []*MultistreamInfo{} },
			func(n *Playlist, e *MultistreamInfo) { n.Edges.MultistreamInfo = append(n.Edges.MultistreamInfo, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRuleGroups; query != nil {
		if err := _q.loadRuleGroups(ctx, query, nodes,
			func(n *Playlist) { n.Edges.RuleGroups = []*PlaylistRuleGroup{} },
			func(n *Playlist, e *PlaylistRuleGroup) { n.Edges.RuleGroups = append(n.Edges.RuleGroups, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *PlaylistQuery) loadVods(ctx context.Context, query *VodQuery, nodes []*Playlist, init func(*Playlist), assign func(*Playlist, *Vod)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*Playlist)
	nids := make(map[uuid.UUID]map[*Playlist]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(playlist.VodsTable)
		s.Join(joinT).On(s.C(vod.FieldID), joinT.C(playlist.VodsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(playlist.VodsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(playlist.VodsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*Playlist]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Vod](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "vods" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *PlaylistQuery) loadMultistreamInfo(ctx context.Context, query *MultistreamInfoQuery, nodes []*Playlist, init func(*Playlist), assign func(*Playlist, *MultistreamInfo)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Playlist)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.MultistreamInfo(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(playlist.MultistreamInfoColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.playlist_multistream_info
		if fk == nil {
			return fmt.Errorf(`foreign-key "playlist_multistream_info" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "playlist_multistream_info" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PlaylistQuery) loadRuleGroups(ctx context.Context, query *PlaylistRuleGroupQuery, nodes []*Playlist, init func(*Playlist), assign func(*Playlist, *PlaylistRuleGroup)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Playlist)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.PlaylistRuleGroup(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(playlist.RuleGroupsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.playlist_rule_groups
		if fk == nil {
			return fmt.Errorf(`foreign-key "playlist_rule_groups" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "playlist_rule_groups" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *PlaylistQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PlaylistQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(playlist.Table, playlist.Columns, sqlgraph.NewFieldSpec(playlist.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, playlist.FieldID)
		for i := range fields {
			if fields[i] != playlist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PlaylistQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(playlist.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = playlist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PlaylistGroupBy is the group-by builder for Playlist entities.
type PlaylistGroupBy struct {
	selector
	build *PlaylistQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PlaylistGroupBy) Aggregate(fns ...AggregateFunc) *PlaylistGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PlaylistGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlaylistQuery, *PlaylistGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PlaylistGroupBy) sqlScan(ctx context.Context, root *PlaylistQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PlaylistSelect is the builder for selecting fields of Playlist entities.
type PlaylistSelect struct {
	*PlaylistQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PlaylistSelect) Aggregate(fns ...AggregateFunc) *PlaylistSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PlaylistSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlaylistQuery, *PlaylistSelect](ctx, _s.PlaylistQuery, _s, _s.inters, v)
}

func (_s *PlaylistSelect) sqlScan(ctx context.Context, root *PlaylistQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
